# 餐厅菜单设计器 — 项目提示词

## 1. 硬性约束

1. **纯前端项目**：无后端服务、无数据库，所有数据持久化使用 `localStorage`。
2. **技术栈锁定**：Angular 18 + TypeScript + RxJS + Vite（Angular CLI）+ Tailwind CSS，不得擅自替换框架或引入其他 UI 库（如 Angular Material、PrimeNG 等）。
3. **端口锁定**：前端开发服务器端口为 `28607`（映射容器 4200），即 `ng serve --port 28607`，不得更改。
4. **英文短名**：Docker Compose 项目名为 `wjemenudesigner`，`.env` 文件中 `COMPOSE_PROJECT_NAME=wjemenudesigner`。
5. **数据存储**：所有用户数据（菜品、分类、布局方案）仅存 `localStorage`，不得引入 IndexedDB、Service Worker 缓存或任何远程 API 调用。
6. **禁止合并职责**：每个文件只承担单一职责，严禁将多个 Service、多个 Component、多个 Model 合并到同一文件。
7. **组件独立文件**：每个 Angular Component 必须有独立的 `.ts`、`.html`、`.css`（或 `.scss`）三个文件，禁止使用内联模板（`template: `）或内联样式（`styles: `）。
8. **严格类型**：所有数据模型必须定义 TypeScript `interface` 或 `enum`，禁止使用 `any` 类型。
9. **RxJS 规范**：异步数据流统一使用 RxJS Observable + BehaviorSubject 模式，组件通过 `async` pipe 消费流数据，禁止在组件中手动 subscribe 不退订。
10. **中文界面**：所有 UI 文本使用中文，代码注释使用中文。
11. **无第三方拖拽库**：拖拽功能必须自研实现（基于原生鼠标/触摸事件 + Angular 指令），不得引入 `@angular/cdk/drag-drop`、`interact.js`、`react-dnd` 等外部拖拽库。
12. **导出为 PNG**：菜单导出功能使用 `html2canvas` 库将菜单画布区域导出为 PNG 图片，不得使用其他截图方案。

---

## 2. 项目主要需求

### 2.1 项目概述

餐厅菜单设计器是一个面向餐厅经营者和设计师的可视化菜单排版工具。用户可以创建菜品、管理菜品分类、选择菜单布局模板，然后通过拖拽方式将菜品放置到菜单画布上，自由调整位置和大小，最终预览并导出为高质量 PNG 图片。整个过程无需后端支持，所有数据保存在浏览器本地存储中。

**目标用户**：餐厅老板、菜单设计师、美食博主
**核心价值**：零门槛快速制作专业级餐厅菜单，无需设计软件经验

### 2.2 数据模型

#### Model 1：MenuItem（菜品）

```typescript
interface MenuItem {
  id: string;                    // UUID
  name: string;                  // 菜品名称
  description: string;           // 菜品描述（限 200 字）
  price: number;                 // 价格（单位：元，保留 2 位小数）
  categoryId: string;            // 所属分类 ID
  image: string;                 // 图片 Data URL 或占位图标识
  isRecommended: boolean;        // 是否推荐
  spiceLevel: SpiceLevel;        // 辣度等级
  isVegetarian: boolean;         // 是否素食
  createdAt: number;             // 创建时间戳
  updatedAt: number;             // 更新时间戳
}
```

#### Model 2：Category（菜品分类）

```typescript
interface Category {
  id: string;          // UUID
  name: string;        // 分类名称
  sortOrder: number;    // 排序权重（数字越小越靠前）
  color: string;       // 分类颜色（HEX，如 #FF6B35）
  icon: string;        // 分类图标标识（emoji 或图标名）
}
```

#### Model 3：MenuLayout（菜单布局）

```typescript
interface MenuLayout {
  id: string;                  // UUID
  name: string;                // 布局方案名称
  templateType: TemplateType;  // 模板类型
  items: LayoutItem[];         // 画布上的菜品项列表
  style: LayoutStyle;          // 布局样式配置
  createdAt: number;           // 创建时间戳
  updatedAt: number;           // 更新时间戳
}
```

#### Model 4：LayoutItem（画布上的菜品项）

```typescript
interface LayoutItem {
  id: string;          // UUID
  menuItemId: string;  // 关联的菜品 ID
  x: number;           // X 坐标（px）
  y: number;           // Y 坐标（px）
  width: number;       // 宽度（px）
  height: number;      // 高度（px）
  fontSize: number;    // 字体大小（px）
  fontStyle: string;   // 字体样式（normal / italic / bold）
}
```

#### Model 5：LayoutStyle（布局样式）

```typescript
interface LayoutStyle {
  id: string;           // UUID
  bgColor: string;      // 背景色（HEX）
  textColor: string;    // 文字颜色（HEX）
  accentColor: string;  // 强调色（HEX）
  fontFamily: string;   // 字体族（如 'serif', 'sans-serif'）
  headerText: string;   // 菜单标题文字
  subtitleText: string; // 菜单副标题文字
}
```

### 2.3 核心页面（≥4 个）

#### 页面 1：设计器主页面（Designer）

左侧为素材面板（菜品列表 + 分类筛选），中间为菜单画布（拖拽区域 + 网格辅助线），右侧为属性面板（选中项的位置、尺寸、字体等属性编辑）。顶部工具栏包含：撤销/重做、切换模板、全屏预览、导出 PNG、保存布局等操作按钮。画布支持缩放和平移。

#### 页面 2：菜品管理页面（Item Management）

以表格 + 卡片双视图模式展示所有菜品。支持创建、编辑、删除菜品。表单包含：名称、描述、价格、分类选择、图片上传（转 Data URL）、推荐标记、辣度选择、素食标记。支持按分类筛选、按名称搜索、按价格排序。

#### 页面 3：分类管理页面（Category Management）

展示所有菜品分类的列表，支持创建新分类（名称、颜色、图标）、编辑已有分类、删除分类（需确认，删除时关联菜品的 categoryId 置空）。支持拖拽排序分类顺序。每个分类显示关联菜品数量统计。

#### 页面 4：模板画廊页面（Template Gallery）

以卡片形式展示 3 种预置菜单布局模板（经典单栏、双栏现代、卡片式），每个模板有缩略预览图和说明文字。点击模板可一键应用到当前布局，也可在此基础上自定义样式（背景色、文字色、强调色、字体、标题文字等）。

#### 页面 5：全屏预览页面（Fullscreen Preview）

全屏展示当前菜单布局的最终效果，模拟打印纸张外观（白色卡片 + 阴影 + A4 比例）。支持缩放查看细节。底部浮动工具栏包含：返回编辑、导出 PNG、打印等操作。支持切换暗色/亮色背景查看菜单在不同环境下的效果。

### 2.4 横切关注点

#### 横切关注点 1：主题切换

- **触达层**：全局主题服务 `ThemeService`（基于 RxJS `BehaviorSubject`），CSS 自定义变量（`--primary-color`、`--bg-color`、`--text-color` 等），全局样式文件 `theme.css`
- **影响范围**：应用外壳（导航栏、侧边栏背景）、菜单预览主题、画布背景、所有表单组件配色
- **实现方式**：`ThemeService` 持有当前主题的 BehaviorSubject，组件订阅主题变化；通过在 `<body>` 上切换 CSS class（如 `theme-light`、`theme-dark`、`theme-warm`）驱动 CSS 变量值变化；菜单预览区域额外支持独立于应用主题的菜单配色方案

#### 横切关注点 2：拖拽状态管理

- **触达层**：`DragService`（RxJS 管理拖拽状态流）、`DraggableDirective`（绑定到可拖拽元素）、`ResizableDirective`（绑定到可调整大小的元素）、画布组件（接收拖拽放置）、属性面板（同步显示选中项属性）
- **状态流**：`DragService` 通过 `BehaviorSubject<DragState>` 管理拖拽阶段（idle / dragging / dropping），通过 `Subject<DropEvent>` 发出放置事件；画布组件订阅放置事件更新 LayoutItem 数组
- **撤销/重做**：每次画布操作（移动、调整大小、删除、添加）都将快照推入 `UndoRedoService` 的操作栈，支持 Ctrl+Z / Ctrl+Shift+Z 撤销重做

### 2.5 共享枚举/常量

#### 枚举组 1：TemplateType

```typescript
enum TemplateType {
  CLASSIC = 'classic',   // 经典单栏
  MODERN = 'modern',     // 双栏现代
  CARD = 'card'          // 卡片式
}
```

- **定义位置**：`frontend/src/app/models/enums.ts`
- **使用位置**：MenuLayout 模型（templateType 字段）、TemplateService（模板切换逻辑）、模板选择器组件（下拉/按钮组）、预览组件（根据类型切换渲染逻辑）

#### 枚举组 2：SpiceLevel

```typescript
enum SpiceLevel {
  NONE = 'none',     // 不辣
  MILD = 'mild',     // 微辣
  MEDIUM = 'medium', // 中辣
  HOT = 'hot'        // 特辣
}
```

- **定义位置**：`frontend/src/app/models/enums.ts`
- **使用位置**：MenuItem 模型（spiceLevel 字段）、菜品表单组件（辣度选择器）、菜品卡片组件（辣度图标展示）、SpiceIcon 共享组件、SpiceLevelPipe 管道

### 2.6 共享组件（≥3 个）

#### 组件 1：`<app-menu-item-card>`

- **功能**：菜品卡片展示组件，显示菜品图片（或占位图）、名称、价格、辣度图标、推荐标签、素食标记
- **输入属性**：`menuItem: MenuItem`、`compact: boolean`（紧凑模式用于画布内展示）
- **使用场景**：菜品列表页面（完整模式）、设计器画布内（紧凑模式）、全屏预览（打印模式）
- **存放位置**：`frontend/src/app/components/common/menu-item-card/`

#### 组件 2：`<app-category-badge>`

- **功能**：分类标签组件，显示分类名称、颜色圆点、图标
- **输入属性**：`category: Category`、`size: 'sm' | 'md' | 'lg'`、`showCount: boolean`、`count: number`
- **使用场景**：菜品列表筛选栏、分类管理页面列表、画布内菜品分类标注
- **存放位置**：`frontend/src/app/components/common/category-badge/`

#### 组件 3：`<app-spice-icon>`

- **功能**：辣度图标组件，根据辣度等级显示不同数量和颜色的辣椒图标
- **输入属性**：`level: SpiceLevel`、`size: number`（图标大小 px）
- **使用场景**：菜品卡片、菜品表单预览区、菜单预览页面
- **存放位置**：`frontend/src/app/components/common/spice-icon/`

#### 组件 4：`<app-template-preview>`

- **功能**：模板缩略预览组件，以缩小比例渲染菜单布局模板的概览效果
- **输入属性**：`templateType: TemplateType`、`style: LayoutStyle`、`items: LayoutItem[]`
- **使用场景**：模板画廊页面的模板卡片、设计器顶部工具栏的模板切换下拉
- **存放位置**：`frontend/src/app/components/common/template-preview/`

### 2.7 Services（共享 Angular 服务）

#### Service 1：`StorageService`

- **职责**：封装 localStorage 读写操作，提供类型安全的 get/set/remove 方法，处理 JSON 序列化/反序列化，内置存储容量检测和数据迁移机制
- **注入层级**：`root`（全局单例）
- **存放位置**：`frontend/src/app/services/storage.service.ts`

#### Service 2：`DragService`

- **职责**：管理拖拽状态机（idle -> dragging -> dropping），通过 RxJS BehaviorSubject 暴露拖拽状态流，记录拖拽源元素信息和当前位置，协调侧边栏（拖出）和画布（拖入）之间的通信
- **注入层级**：`root`（全局单例）
- **存放位置**：`frontend/src/app/services/drag.service.ts`

#### Service 3：`UndoRedoService`

- **职责**：维护操作历史栈（undoStack / redoStack），每次画布操作推入快照，支持撤销（undo）和重做（redo），通过 RxJS Subject 通知组件按钮可用状态变化
- **注入层级**：`root`（全局单例）
- **存放位置**：`frontend/src/app/services/undo-redo.service.ts`

#### Service 4：`MenuService`

- **职责**：菜品和分类的 CRUD 业务逻辑，基于 StorageService 进行数据持久化，提供菜品列表、分类列表的 RxJS Observable 流，处理分类删除时的关联菜品清理
- **注入层级**：`root`（全局单例）
- **存放位置**：`frontend/src/app/services/menu.service.ts`

#### Service 5：`TemplateService`

- **职责**：管理 3 种预置菜单模板的定义和应用逻辑，提供模板列表查询、模板应用（将模板的默认 LayoutItem 和 LayoutStyle 写入当前布局）、自定义样式保存
- **注入层级**：`root`（全局单例）
- **存放位置**：`frontend/src/app/services/template.service.ts`

---

## 3. 文件结构强制清单

```
frontend/src/
├── app/
│   ├── services/
│   │   ├── storage.service.ts          # localStorage 封装
│   │   ├── drag.service.ts             # 拖拽状态管理
│   │   ├── undo-redo.service.ts        # 撤销/重做栈
│   │   ├── menu.service.ts             # 菜品/分类 CRUD
│   │   └── template.service.ts         # 模板管理
│   ├── models/
│   │   ├── menu-item.model.ts          # MenuItem 接口
│   │   ├── category.model.ts           # Category 接口
│   │   ├── menu-layout.model.ts        # MenuLayout + LayoutItem + LayoutStyle 接口
│   │   └── enums.ts                    # TemplateType + SpiceLevel 枚举
│   ├── components/
│   │   ├── common/
│   │   │   ├── menu-item-card/         # 菜品卡片组件
│   │   │   ├── category-badge/         # 分类标签组件
│   │   │   ├── spice-icon/             # 辣度图标组件
│   │   │   ├── template-preview/       # 模板缩略预览
│   │   │   └── confirm-dialog/         # 确认对话框
│   │   ├── canvas/
│   │   │   ├── menu-canvas/            # 菜单画布主组件
│   │   │   ├── canvas-item/            # 画布内单个菜品项
│   │   │   └── grid-overlay/           # 网格辅助线覆盖层
│   │   ├── sidebar/
│   │   │   ├── item-panel/             # 左侧菜品素材面板
│   │   │   ├── property-panel/         # 右侧属性编辑面板
│   │   │   └── category-panel/         # 分类筛选面板
│   │   └── preview/
│   │       ├── fullscreen-preview/     # 全屏预览组件
│   │       └── print-preview/          # 打印效果预览
│   ├── pages/
│   │   ├── designer/                   # 设计器主页面
│   │   ├── menu-list/                  # 菜品管理页面
│   │   ├── item-management/            # 菜品详细管理（创建/编辑）
│   │   ├── category-management/        # 分类管理页面
│   │   ├── template-gallery/           # 模板画廊页面
│   │   └── settings/                   # 设置页面（主题、数据导入导出）
│   ├── directives/
│   │   ├── draggable.directive.ts      # 拖拽指令
│   │   └── resizable.directive.ts      # 可调整大小指令
│   ├── pipes/
│   │   ├── currency.pipe.ts            # 价格格式化管道（如 ¥12.00）
│   │   └── spice-level.pipe.ts         # 辣度文字转换管道
│   ├── guards/
│   │   └── unsaved-changes.guard.ts    # 未保存变更离开守卫
│   ├── app.component.ts
│   ├── app.component.html
│   ├── app.component.css
│   ├── app.config.ts
│   └── app.routes.ts
├── assets/
│   ├── icons/                          # SVG 图标资源
│   └── images/                         # 占位图、默认模板背景
├── styles/
│   ├── theme.css                       # 主题 CSS 变量定义
│   ├── canvas.css                      # 画布相关样式
│   └── animations.css                  # 动画过渡效果
└── environments/
    └── environment.ts                  # 环境配置
```

**严禁合并职责到单一文件**：每个 Service、每个 Component、每个 Directive、每个 Pipe 必须有独立文件。不得出现以下情况：
- 将 `StorageService` 和 `MenuService` 写在同一个文件中
- 将 `MenuItemCard` 和 `CategoryBadge` 写在同一个文件中
- 将 `DraggableDirective` 和 `ResizableDirective` 写在同一个文件中
- 将多个 Model 接口分散在不同文件中（应集中在一个 model 文件中按职责分开）

---

## 4. 核心流程

### 流程 1：创建菜品

1. 用户进入菜品管理页面，点击"新建菜品"按钮
2. 弹出/跳转菜品表单，填写：名称、描述、价格、选择分类、上传图片（转为 Data URL）、选择辣度、勾选推荐和素食标记
3. 表单校验通过后，`MenuService` 生成 UUID、设置时间戳、调用 `StorageService` 写入 localStorage
4. 菜品列表实时刷新，新菜品出现在列表中

### 流程 2：分类管理

1. 用户进入分类管理页面，查看现有分类列表（显示关联菜品数量）
2. 创建新分类：填写名称、选择颜色、选择图标
3. 编辑分类：修改名称、颜色、图标
4. 删除分类：弹出确认对话框，确认后将关联菜品的 `categoryId` 清空，删除分类数据
5. 拖拽排序：调整分类的 `sortOrder`

### 流程 3：选择模板并自定义

1. 用户进入模板画廊页面，浏览 3 种模板的缩略预览
2. 点击某个模板，`TemplateService` 生成该模板的默认 `LayoutItem[]` 和 `LayoutStyle`
3. 应用到设计器画布，用户可在属性面板中自定义：背景色、文字色、强调色、字体、标题、副标题

### 流程 4：拖拽排版

1. 在设计器主页面，左侧素材面板按分类展示菜品列表
2. 用户从左侧面板拖拽菜品卡片到画布区域，`DragService` 管理拖拽状态
3. 放置到画布后，生成新的 `LayoutItem`（默认尺寸和位置），画布上渲染为 `CanvasItem` 组件
4. 画布内的菜品项可拖拽移动位置、拖拽边角调整大小
5. 选中某个菜品项后，右侧属性面板显示其详细属性（坐标、尺寸、字体等），可精确编辑
6. 每次操作自动推入 `UndoRedoService` 操作栈，支持 Ctrl+Z 撤销、Ctrl+Shift+Z 重做
7. 网格辅助线可开关，帮助对齐元素

### 流程 5：预览与导出

1. 用户点击"预览"按钮，进入全屏预览页面
2. 预览页面以 A4 纸张比例展示菜单最终效果，背景模拟桌面/大理石纹理
3. 用户确认无误后，点击"导出 PNG"
4. 使用 `html2canvas` 将菜单画布区域捕获为 Canvas，调用 `toDataURL('image/png')` 生成图片
5. 创建临时 `<a>` 标签触发下载，文件名格式：`菜单_{日期}_{时间}.png`

---

## 5. 测试样例数据

### 预置分类（3 个）

| 分类名 | sortOrder | color | icon |
|--------|-----------|-------|------|
| 开胃菜 | 1 | #FF6B35 | appetizer |
| 主菜 | 2 | #C62828 | main-course |
| 甜品 | 3 | #AD1457 | dessert |
| 饮品 | 4 | #1565C0 | beverage |

### 预置菜品（15 个，含中西餐）

| 菜品名 | 分类 | 价格 | 辣度 | 推荐 | 素食 |
|--------|------|------|------|------|------|
| 凯撒沙拉 | 开胃菜 | 38.00 | none | 否 | 否 |
| 蒜香面包 | 开胃菜 | 22.00 | none | 否 | 是 |
| 四川口水鸡 | 开胃菜 | 45.00 | medium | 是 | 否 |
| 香煎三文鱼 | 主菜 | 128.00 | none | 是 | 否 |
| 黑椒牛排 | 主菜 | 168.00 | mild | 是 | 否 |
| 宫保鸡丁 | 主菜 | 58.00 | medium | 否 | 否 |
| 意式海鲜意面 | 主菜 | 88.00 | none | 否 | 否 |
| 麻婆豆腐 | 主菜 | 38.00 | hot | 否 | 是 |
| 素食咖喱 | 主菜 | 48.00 | mild | 否 | 是 |
| 提拉米苏 | 甜品 | 42.00 | none | 是 | 否 |
| 芒果布丁 | 甜品 | 32.00 | none | 否 | 否 |
| 抹茶冰淇淋 | 甜品 | 28.00 | none | 否 | 是 |
| 经典莫吉托 | 饮品 | 35.00 | none | 否 | 是 |
| 鲜榨橙汁 | 饮品 | 25.00 | none | 否 | 是 |
| 拿铁咖啡 | 饮品 | 32.00 | none | 否 | 是 |

### 预置模板（3 个）

#### 模板 1：经典单栏（Classic）
- 类型：`TemplateType.CLASSIC`
- 布局：单栏垂直排列，分类标题分隔，菜品左对齐名称 + 右对齐价格（带虚线引导符）
- 默认样式：米白色背景（#FFF8F0）、深棕文字（#3E2723）、金色强调色（#C6953B）、衬线字体

#### 模板 2：双栏现代（Modern）
- 类型：`TemplateType.MODERN`
- 布局：左右双栏等宽，每个分类占据一个区块，菜品卡片式排列，价格用强调色突出
- 默认样式：纯白背景（#FFFFFF）、深灰文字（#212121）、珊瑚红强调色（#FF6B6B）、无衬线字体

#### 模板 3：卡片式（Card）
- 类型：`TemplateType.CARD`
- 布局：网格式卡片排列，每个菜品为独立卡片（含图片区域、名称、描述、价格），按分类分组
- 默认样式：深色背景（#1A1A2E）、白色文字（#EAEAEA）、霓虹紫强调色（#7B2FF7）、无衬线字体

---

## 6. 代码存放规则

这是纯前端项目，无后端代码。

前端代码放在 `frontend/` 目录下：

- 页面组件放在 `frontend/src/app/pages/`
- 共享组件放在 `frontend/src/app/components/`
- 服务放在 `frontend/src/app/services/`
- 数据模型放在 `frontend/src/app/models/`
- 指令放在 `frontend/src/app/directives/`
- 管道放在 `frontend/src/app/pipes/`
- 路由守卫放在 `frontend/src/app/guards/`
- 全局样式放在 `frontend/src/styles/`
- 静态资源放在 `frontend/src/assets/`
- 环境配置放在 `frontend/src/environments/`
- 测试样例数据（预置的分类、菜品、模板）写在 `frontend/src/app/services/template.service.ts` 和 `frontend/src/app/services/menu.service.ts` 的初始化方法中

---

## 7. 部署要求

### 7.1 开发环境

- 启动命令：`cd frontend && npm install && ng serve --port 28607`
- 访问地址：`http://localhost:28607`

### 7.2 Docker Compose 部署

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=wjemenudesigner`
- `frontend/Dockerfile`：前端项目构建与运行镜像（多阶段构建：Node 构建 + Nginx 托管）
- `frontend/nginx.conf`：Nginx 配置，处理 SPA 前端路由
- `.env.example`：环境变量示例文件

docker-compose.yml 要求：
- **不要写 `version:` 字段**（Compose V2 已废弃）
- 顶层声明 `name: wjemenudesigner` 作为兜底
- 使用 `container_name` 命名容器
- 端口映射：`28607:80`
- 支持通过 `docker compose up -d` 一条命令启动

### 7.3 docker-compose.yml 参考骨架

```yaml
name: wjemenudesigner

services:
  frontend:
    build: ./frontend
    container_name: ${COMPOSE_PROJECT_NAME:-wjemenudesigner}-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-28607}:80"
```

### 7.4 .env.example 参考

```dotenv
# Compose 项目名（小写字母/数字/-/_），避免中文目录名导致 project name 为空
COMPOSE_PROJECT_NAME=wjemenudesigner
FRONTEND_PORT=28607
```

### 7.5 自检清单

1. 在 **中文目录名** 下执行 `docker compose config --quiet` 不报错
2. 启动后 `docker compose ps` 显示容器运行正常
3. 浏览器访问 `http://localhost:28607` 能看到页面
4. 所有功能（菜品CRUD、分类管理、拖拽排版、预览、导出PNG）正常工作
5. 刷新页面后数据仍在（localStorage 持久化）

---

## 8. README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能（列表）
- 快速启动方式（Docker Compose 一键部署命令）
- 本地开发方式（前端启动命令）
- 访问地址
- 技术栈（表格展示）
- 项目目录结构（代码块展示）
- 核心功能截图说明（占位）
- 环境变量说明
- Docker 部署说明（端口映射、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示
- 不要保留脚手架默认 README 内容
- README 中务必告知用户首次启动前执行 `cp .env.example .env`

---

## 9. Git 要求

代码编写完成后初始化 Git + 创建 `.gitignore`，`.gitignore` 内容按项目技术栈适配：

```
node_modules/
dist/
.angular/
.env
.DS_Store
*.log
.idea/
.vscode/
```
